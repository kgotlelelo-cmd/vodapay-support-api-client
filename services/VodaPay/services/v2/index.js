const applyToken = require('./applyToken');
const inquiryPayment = require('./inquiryPayment');
const inquiryUserInfo = require('./inquiryUserInfo');
const pay = require('./pay');
module.exports = {
    applyToken,
    inquiryPayment,
    inquiryUserInfo,
    pay
}